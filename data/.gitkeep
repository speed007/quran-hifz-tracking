# This directory is mounted into the container at /app/data and holds the
# SQLite database file (hifz.db) so data survives container recreation.
# The .gitkeep ensures the directory ships with the repo.
